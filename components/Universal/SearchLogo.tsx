import * as React from 'react';
import { View } from 'react-native';
import MaterialIcons from "react-native-vector-icons/MaterialIcons";

const SearchLogo = () => {

  return (
    <View>

    <MaterialIcons name="search" size={36} color={'black'}/>
    </View>
  );
};

export default SearchLogo;